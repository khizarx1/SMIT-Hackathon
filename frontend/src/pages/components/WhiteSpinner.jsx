import { LoadingOutlined } from '@ant-design/icons';

export const whiteSpinner = (
        <LoadingOutlined
            style={{
                fontSize: 20,
                color: 'white',
            }}
            spin
        />
    );